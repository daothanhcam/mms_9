<% provide :title, t("project.list_project") %>
<h1><%= t "project.title.index" %></h1>
<p><%= link_to t("project.create_new"), new_admin_project_path, class: "btn btn-success" %></p>

<table>
  <%= render partial: "project", collection: @projects %>
</table>

<%= will_paginate %>
